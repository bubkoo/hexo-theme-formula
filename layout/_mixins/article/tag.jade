mixin tag(post)
  if post.tags && post.tags.length
    - var tags = [];
    each tag in post.tags.toArray()
      - tags.push(link_to(url_for(tag.path), tag.name))

    div.tags
      != tags.join(' ')
